spring:
  datasource:
    url: jdbc:mysql://localhost/uat_1
    username: root
    password:
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update # Options: validate | update | create | create-drop
    show-sql: true

  data:
    redis:
      host: localhost
      password:
      port: 6379

server:
  port: 3002
